source "https://rubygems.org"

ruby ">= 3.1"

gem "rails", "~> 7.1"
gem "sqlite3", "~> 1.4"

# SQLite é usado para manter o lab leve. A coluna `translations` é json (o SQLite
# suporta json nativo). Para JSONB real você pode trocar por Postgres (pg), mas
# não é necessário para completar o lab.

# Descomente se optar por resolver via gem em vez de acessores manuais:
# gem "mobility", "~> 1.3"

group :development, :test do
  gem "rspec-rails", "~> 6.1"
end
